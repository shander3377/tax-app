// @ts-nocheck
import { H5, Tabs, Text } from "tamagui";

import { MyStack } from "../../components/MyStack";

export default function Tab1() {
  return (
    <Text>new</Text>
    //write new regime code here
    //have a button in end which transfers screen to result's screen. eg:
    // <Button onPress={() => router.push("/results")}>Go to tabs page</Button>
    
    //tamagui ki site pe jaake Ui waale section pe jaake inputs wagera dekhlena, and core waale section mai jaake shorthands dekhlena (Search them)
    //kisi bhi component mai theme="" karke dekuna, usme dark_accent, light_accent, dark, light aur baaki component wise themes bhi h, tamagui ki website pe top bar pe theme karke ek section hoga usse dekh skte ho
  );
}
